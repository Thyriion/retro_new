import { View, Text } from 'react-native'
import React from 'react'
import { TextInput } from 'react-native-paper'

export default function Textinput({ text, style }) {
    return (
        <TextInput></TextInput>
    )
}